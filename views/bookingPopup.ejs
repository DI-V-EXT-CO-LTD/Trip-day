<!-- <div id="bookingPopup" class="popup">
  <div class="popup-content">
    <span class="close">&times;</span>
    <h2>Booking Details</h2>
    <p><strong>APIS Code:</strong> <%= booking.APISCODE %></p>
    <p><strong>Hotel:</strong> <%= booking.Hotel %></p>
    <p><strong>Amount:</strong> $<%= booking.Amount.toFixed(2) %></p>
    <p><strong>Use Date:</strong> <%= booking.useDate.toLocaleDateString() %></p>
    <p><strong>Created At:</strong> <%= booking.createAt.toLocaleDateString() %></p>
    <% if (booking.CustomerInfo && booking.CustomerInfo.length > 0) { %>
      <h3>Customer Information</h3>
      <% booking.CustomerInfo.forEach((customer, index) => { %>
        <div>
          <h4>Customer <%= index + 1 %></h4>
          <% for (const [key, value] of Object.entries(customer)) { %>
            <p><strong><%= key %>:</strong> <%= value %></p>
          <% } %>
        </div>
      <% }) %>
    <% } %>
  </div>
</div> -->